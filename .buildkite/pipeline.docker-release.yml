steps:
  - block: "Create :docker: release?"
    fields:
      - key: release-name
        required: true
        text: Release Name (This name WILL be seen by customers)

  - label: ":rocket: :pipeline: Upload docker release pipeline"
    commands:
      - .buildkite/generate-pipeline-docker-release.sh | buildkite-agent pipeline upload
