[package]
name = "noria-mir"
version = "0.7.0"
edition = "2018"
authors = ["The Noria developers <noria@pdos.csail.mit.edu>"]
license = "MIT OR Apache-2.0"

description = "Internal noria-server crate"
repository = "https://github.com/mit-pdos/noria.git"
homepage = "https://pdos.csail.mit.edu/noria"

keywords = ["internal"]

[badges]
maintenance = { status = "experimental" }

[dependencies]
nom-sql = { path = "../../../nom-sql" }
regex = "1.5"
lazy_static = "1.4"
petgraph = { version = "0.5", features = ["serde-1"] }
itertools = "0.10"
tracing = { version = "0.1", features = ["release_max_level_debug"] }
noria = { path = "../../noria" }
noria-errors = { path = "../../errors" }
serde = { version = "1.0.8", features = ["rc", "derive"] }
noria-data = { path = "../../../noria/data" }

# local deps
common = { path = "../common", package = "noria-common" }
dataflow = { path = "../dataflow", package = "noria-dataflow" }

[dev-dependencies]
readyset-logging = { path = "../../../readyset-logging" }
