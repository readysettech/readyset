# A config with no upstream.  This is a dummy placeholder, since the pipeline auto-selects a compose
# file based on the name "docker-compose.ci-{{ upstream}}.yml"

services:
  app:
    environment:
      - ALLOW_UNAUTHENTICATED_CONNECTIONS=true
