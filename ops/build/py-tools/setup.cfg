[metadata]
name = CFN Deploy
version = 0.0.1
author = Sasha Fox
author_email = sasha@readyset.io

[options]
packages = cfn-deploy
install_requires =
    pyyaml
python_requires = >= 3.6

[options.entry_points]
console_scripts =
    main = cfndeploy:some_func