steps:
  - name: ":node:"
    command: pnpm run ci
    plugins:
      - docker-compose#v3.10.0:
          run: app
          config: docker-compose.yml
    env:
      READYSET_SUBPROJECT_ROOT: readyset-cloud/
